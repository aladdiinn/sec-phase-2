# ── SecurePulse Backend ──────────────────────────────────────
flask>=3.0.3
flask-sqlalchemy>=3.1.1
flask-socketio>=5.3.6
psycopg[binary]>=3.1.0
python-dotenv>=1.0.1
pyjwt>=2.8.0
werkzeug>=3.0.3
eventlet>=0.36.1
gunicorn>=22.0.0
sqlalchemy>=2.0.30
requests>=2.32.3

# ── Phase 2 additions ─────────────────────────────────────────
pyotp>=2.9.0           # Real TOTP/MFA
qrcode[pil]>=7.4.2     # QR code generation for MFA setup
Pillow>=10.0.0         # Image support for QR codes
reportlab>=4.0.0       # Compliance PDF generation
hashlib                # Chain of custody signing (stdlib)

# ── Agent (install on monitored servers) ─────────────────────
psutil>=5.9.8
